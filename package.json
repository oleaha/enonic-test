{
  "scripts": {
    "build": "concurrently \"npm-run-all build:*\"",
    "build:js": "babel src/main/resources --out-dir=build/resources/main",
    "build:test": "babel src/test/resources --out-dir=build/resources/test",
    "test": "babel src/test/resources --out-dir=build/resources/test"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "concurrently": "^8.2.1",
    "npm-run-all": "^4.1.5"
  }
}
